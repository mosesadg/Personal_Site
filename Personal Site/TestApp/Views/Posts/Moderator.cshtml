@model TestApp.Models.Post
@{
    ViewBag.Title = "Moderator";
    Layout = "~/Views/Shared/_Layout.cshtml";
}


<div class="joobstrap_section sourrounding_wrapper" id="main_top">
    <div class="inner_wrapper">
        <h3> <div class="joobstrap_section sourrounding_wrapper" id="main_top">
    <div class="inner_wrapper">
        <h3> Moderator</h3>

        <p>
            @Html.ActionLink("Delete Comment", "DeletComment")
        </p>
        <table class="table">
            <tr>
                
                <th>
                    @Html.DisplayNameFor(model => model.Title)
                </th>
                
                <th>               
                        @Html.DisplayNameFor(model => model.Slug)
                    </th>

                    <th></th>
</tr>

            @foreach (var item in Model)
            {
                <tr>
                    
                    
                    <td>
                        @Html.DisplayFor(modelItem => item.Title)
                    </td>
                    
                    
               <td>
                    @Html.DisplayFor(modelItem => item.Slug)
                </td>
                <td>
                                        
                    @Html.ActionLink("Delete", "Delete", new { id = item.Id })
                </td>
            </tr>
            }

            </table>
        </div>
    </div>



            @foreach (var item in Model)
            {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => item.Created)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Updated)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Title)
                    </td>

                    <!-- <td>
                                                <div class="excerpt">
                                                    @Html.DisplayFor(modelItem => item.Body)
                                                </div>
                    </td>-->

                    <td>
                        @Html.DisplayFor(modelItem => item.MediaURL)
                    </td>
                    <!-- <td>
                         @Html.DisplayFor(modelItem => item.Slug)
                     </td>-->
                    <td>
                        @Html.ActionLink("Edit", "Edit", new { id = item.Id }) |
                        @Html.ActionLink("Details", "Details", new { Slug = item.Slug }) |
                        @Html.ActionLink("Delete", "Delete", new { id = item.Id })
                    </td>
                </tr>
            }

        </table>
    </div>
</div>
<style type="text/css">
    div.excerpt {
        height: 2.2em;
        overflow: hidden;
    }

        div.excerpt:hover {
            background-color: lightgrey;
        }
</style>